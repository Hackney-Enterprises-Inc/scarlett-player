---
"@scarlett-player/watermark": patch
---

Add `imageHeight` and `padding` config options to watermark plugin. Image size is now controlled by `imageHeight` (default 40px) instead of being derived from `fontSize`. Edge offset is controlled by `padding` (default 10px, 40px for bottom positions to clear player controls).
